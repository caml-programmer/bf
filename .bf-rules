(define (build args)
    (log-command "./configure" (path-concat dest-dir top-dir))
  (log-command "make" "environment")
  (log-command "make"))

(define (install args)
    (log-command "make" "install"))
